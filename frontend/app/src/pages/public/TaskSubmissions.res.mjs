// Generated by ReScript, PLEASE EDIT WITH CARE

import * as React from "react";
import * as NextLink from "../../bindings/NextLink.res.mjs";
import * as TasksApi from "../../api/tasks/TasksApi.res.mjs";
import * as Belt_Array from "@rescript/runtime/lib/es6/Belt_Array.js";
import * as Stdlib_JsExn from "@rescript/runtime/lib/es6/Stdlib_JsExn.js";
import * as Stdlib_Promise from "@rescript/runtime/lib/es6/Stdlib_Promise.js";
import * as JsxRuntime from "react/jsx-runtime";
import * as TaskSubmissionsModuleScss from "./TaskSubmissions.module.scss";

let page = TaskSubmissionsModuleScss.page;

let top = TaskSubmissionsModuleScss.top;

let h1 = TaskSubmissionsModuleScss.h1;

let sub = TaskSubmissionsModuleScss.sub;

let card = TaskSubmissionsModuleScss.card;

let row = TaskSubmissionsModuleScss.row;

let left = TaskSubmissionsModuleScss.left;

let title = TaskSubmissionsModuleScss.title;

let meta = TaskSubmissionsModuleScss.meta;

let right = TaskSubmissionsModuleScss.right;

let badge = TaskSubmissionsModuleScss.badge;

let btn = TaskSubmissionsModuleScss.btn;

let S = {
  page: page,
  top: top,
  h1: h1,
  sub: sub,
  card: card,
  row: row,
  left: left,
  title: title,
  meta: meta,
  right: right,
  badge: badge,
  btn: btn
};

function errMsg(e) {
  let m = Stdlib_JsExn.message(e);
  if (m !== undefined) {
    return m;
  } else {
    return "error";
  }
}

function TaskSubmissions(props) {
  let taskId = props.taskId;
  let match = React.useState(() => []);
  let setItems = match[1];
  let match$1 = React.useState(() => "");
  let setErr = match$1[1];
  let err = match$1[0];
  let match$2 = React.useState(() => true);
  let setLoading = match$2[1];
  React.useEffect(() => {
    if (taskId === "") {
      setLoading(param => false);
    } else {
      setLoading(param => true);
      Stdlib_Promise.$$catch(TasksApi.mySubmissions(taskId, 50, 0).then(r => {
        setItems(param => r.items);
        setErr(param => "");
        setLoading(param => false);
        return Promise.resolve();
      }), e => {
        setErr(param => errMsg(e));
        setLoading(param => false);
        return Promise.resolve();
      });
    }
  }, [taskId]);
  let tmp = err === "" ? null : JsxRuntime.jsx("div", {
      children: "Error: " + err,
      className: sub
    });
  return JsxRuntime.jsxs("div", {
    children: [
      JsxRuntime.jsxs("div", {
        children: [
          JsxRuntime.jsxs("div", {
            children: [
              JsxRuntime.jsx("div", {
                children: "My submissions",
                className: h1
              }),
              JsxRuntime.jsx("div", {
                children: match$2[0] ? "Loading..." : "Task id: " + taskId,
                className: sub
              })
            ]
          }),
          JsxRuntime.jsx(NextLink.make, {
            href: "/task/" + taskId,
            className: btn,
            children: "Back"
          })
        ],
        className: top
      }),
      tmp,
      JsxRuntime.jsx("div", {
        children: Belt_Array.mapWithIndex(match[0], (i, it) => JsxRuntime.jsxs("div", {
          children: [
            JsxRuntime.jsxs("div", {
              children: [
                JsxRuntime.jsx("div", {
                  children: "#" + (i + 1 | 0).toString() + " • " + it.created_at,
                  className: title
                }),
                JsxRuntime.jsx("div", {
                  children: "score " + it.score.toString() + " • " + it.passed_tests.toString() + "/" + it.total_tests.toString(),
                  className: meta
                })
              ],
              className: left
            }),
            JsxRuntime.jsxs("div", {
              children: [
                JsxRuntime.jsx("div", {
                  children: it.status,
                  className: badge
                }),
                JsxRuntime.jsx(NextLink.make, {
                  href: "/submission/" + it.id,
                  className: btn,
                  children: "Open"
                })
              ],
              className: right
            })
          ],
          className: row
        }, it.id)),
        className: card
      })
    ],
    className: page
  });
}

let make = TaskSubmissions;

export {
  S,
  errMsg,
  make,
}
/* page Not a pure module */
