// Generated by ReScript, PLEASE EDIT WITH CARE

import * as React from "react";
import * as NextLink from "../../bindings/NextLink.res.mjs";
import * as TopicsApi from "../../api/topics/TopicsApi.res.mjs";
import * as Stdlib_JsExn from "@rescript/runtime/lib/es6/Stdlib_JsExn.js";
import * as Stdlib_Promise from "@rescript/runtime/lib/es6/Stdlib_Promise.js";
import * as JsxRuntime from "react/jsx-runtime";
import * as HomeModuleScss from "./Home.module.scss";

let page = HomeModuleScss.page;

let top = HomeModuleScss.top;

let h1 = HomeModuleScss.h1;

let sub = HomeModuleScss.sub;

let grid = HomeModuleScss.grid;

let card = HomeModuleScss.card;

let cardTitle = HomeModuleScss.cardTitle;

let cardMeta = HomeModuleScss.cardMeta;

let S = {
  page: page,
  top: top,
  h1: h1,
  sub: sub,
  grid: grid,
  card: card,
  cardTitle: cardTitle,
  cardMeta: cardMeta
};

function errMsg(e) {
  let m = Stdlib_JsExn.message(e);
  if (m !== undefined) {
    return m;
  } else {
    return "error";
  }
}

function Home(props) {
  let match = React.useState(() => []);
  let setTopics = match[1];
  let topics = match[0];
  let match$1 = React.useState(() => "");
  let setErr = match$1[1];
  let err = match$1[0];
  let match$2 = React.useState(() => true);
  let setLoading = match$2[1];
  React.useEffect(() => {
    setLoading(param => true);
    Stdlib_Promise.$$catch(TopicsApi.list().then(ts => {
      setTopics(param => ts);
      setErr(param => "");
      setLoading(param => false);
      return Promise.resolve();
    }), e => {
      setErr(param => errMsg(e));
      setLoading(param => false);
      return Promise.resolve();
    });
  }, []);
  let tmp = err === "" ? null : JsxRuntime.jsx("div", {
      children: "Error: " + err,
      className: sub
    });
  return JsxRuntime.jsxs("div", {
    children: [
      JsxRuntime.jsxs("div", {
        children: [
          JsxRuntime.jsx("div", {
            children: "Topics",
            className: h1
          }),
          JsxRuntime.jsx("div", {
            children: match$2[0] ? "Loading..." : topics.length.toString() + " topics",
            className: sub
          }),
          tmp
        ],
        className: top
      }),
      JsxRuntime.jsx("div", {
        children: topics.map(t => JsxRuntime.jsx(NextLink.make, {
          href: "/topic/" + t.id,
          className: card,
          children: JsxRuntime.jsxs(JsxRuntime.Fragment, {
            children: [
              JsxRuntime.jsx("div", {
                children: t.title,
                className: cardTitle
              }),
              JsxRuntime.jsx("div", {
                children: t.subtitle === "" ? "Open" : t.subtitle,
                className: cardMeta
              })
            ]
          })
        }, t.id)),
        className: grid
      })
    ],
    className: page
  });
}

let make = Home;

export {
  S,
  errMsg,
  make,
}
/* page Not a pure module */
