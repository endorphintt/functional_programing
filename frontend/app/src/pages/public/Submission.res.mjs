// Generated by ReScript, PLEASE EDIT WITH CARE

import * as React from "react";
import * as Stdlib_JsExn from "@rescript/runtime/lib/es6/Stdlib_JsExn.js";
import * as Stdlib_Promise from "@rescript/runtime/lib/es6/Stdlib_Promise.js";
import * as SubmissionsApi from "../../api/submissions/SubmissionsApi.res.mjs";
import * as JsxRuntime from "react/jsx-runtime";
import * as SubmissionModuleScss from "./Submission.module.scss";

let page = SubmissionModuleScss.page;

let h1 = SubmissionModuleScss.h1;

let sub = SubmissionModuleScss.sub;

let card = SubmissionModuleScss.card;

let pre = SubmissionModuleScss.pre;

let S = {
  page: page,
  h1: h1,
  sub: sub,
  card: card,
  pre: pre
};

function errMsg(e) {
  let m = Stdlib_JsExn.message(e);
  if (m !== undefined) {
    return m;
  } else {
    return "error";
  }
}

function Submission(props) {
  let submissionId = props.submissionId;
  let match = React.useState(() => {});
  let setItem = match[1];
  let item = match[0];
  let match$1 = React.useState(() => "");
  let setErr = match$1[1];
  let err = match$1[0];
  let match$2 = React.useState(() => true);
  let setLoading = match$2[1];
  React.useEffect(() => {
    if (submissionId === "") {
      setLoading(param => false);
    } else {
      setLoading(param => true);
      Stdlib_Promise.$$catch(SubmissionsApi.getOne(submissionId).then(s => {
        setItem(param => s);
        setErr(param => "");
        setLoading(param => false);
        return Promise.resolve();
      }), e => {
        setErr(param => errMsg(e));
        setLoading(param => false);
        return Promise.resolve();
      });
    }
  }, [submissionId]);
  let tmp = err === "" ? null : JsxRuntime.jsx("div", {
      children: "Error: " + err,
      className: sub
    });
  return JsxRuntime.jsxs("div", {
    children: [
      JsxRuntime.jsx("div", {
        children: match$2[0] ? "Loading..." : (
            item !== undefined ? "Submission " + item.id : "Submission " + submissionId
          ),
        className: h1
      }),
      tmp,
      item !== undefined ? JsxRuntime.jsxs("div", {
          children: [
            JsxRuntime.jsx("div", {
              children: "status: " + item.status + " | score " + item.score.toString() + " | passed " + item.passed_tests.toString() + "/" + item.total_tests.toString(),
              className: sub
            }),
            JsxRuntime.jsx("pre", {
              children: JSON.stringify(item.result, undefined, 2),
              className: pre
            })
          ],
          className: card
        }) : null
    ],
    className: page
  });
}

let make = Submission;

export {
  S,
  errMsg,
  make,
}
/* page Not a pure module */
